---
title: Геометрический смысл
created: 2026-02-23
tags:
  - calculus
links:
  - "[[Комплексная производная]]"
---
Пусть функция $w=f(z)$ является аналитической в точке $z_{0}$ и $f'(z_{0})\neq 0$. Функция $\omega=f(z)$ отображает точку $z_{0}$ плоскости $z$ в точку $w_{0}=f(z_{0})$ плоскости $w$.

Точка $z=z_{0}+\Delta z$ из окрестности точки $z_{0}$ перемещается по непрерывной кривой $l$. Тогда соответствующая точка $w=w_{0}+\Delta w$ в плоскости $w$ будет перемещаться к точке $w_{0}$ по некоторой кривой $L$, которая является отображением кривой $l$ в плоскости $w$.

По определению производной

$$
f'(z_{0})= \lim_{ \Delta z \to \infty }  \frac{\Delta w}{\Delta z}
$$


$$
|f'(z_{0})|= \left\lvert  \lim_{ \Delta z \to 0 } \frac{\Delta w}{\Delta z}   \right\rvert =\lim_{ \Delta z \to 0 }  \left\lvert  \frac{\Delta w}{\Delta z}  \right\rvert= \lim_{ \Delta z \to 0 } \frac{|\Delta w|}{|\Delta z|}
$$
$\Delta z=z-z_{0}$ - расстояние между $z_{0}$ и $z_{0}+\Delta z$
$\Delta w=|w-w_{0}|$ - расстояние между $w$ и $w_{0}$

Предел не зависит от выбора кривой $l$, проходящей через точку $z_{0}$. Следовательно, предел в точке $z_{0}$ постоянен, т.е. одинаков во всех направлениях.

**Геометрический смысл модуля производной:** величина $|f'(z_{0})|$ определяет коэффициент растяжения (подобия) в точке $z_{0}$ при отображении $w=f(z)$.

Если $|f'(z_{0})|>1$, то $|f'(z_{0})|$ называется *коэффициентом растяжения*
Если $|f'(z_{0})|<1$, то $f'(z_{0})$ называется *коэффициентом сжатия*.

Рассмотрим аргумент производной в точке $z_{0}$:
$$
\arg f'(z_{0})=\lim_{ \Delta z \to 0 } \arg \frac{\Delta w}{\Delta z}=\lim_{ \Delta z \to 0 } (\arg\Delta w-\arg\Delta z)=\lim_{ \Delta z \to 0 } \arg\Delta w-\lim_{ \Delta z \to 0 } \arg\Delta z=\alpha_{2}-\alpha_{1},
$$
где $\alpha_{1},\alpha_{2}$ - углы, которые образуют касательные к кривым $l$ и $L$ соответственно в точках $z_{0}$ и $w_{0}$ с положительными направлениями действительных осей на плоскостях $z$ и $w$.
$$
\alpha_{2}=\alpha_{1}+\arg f'(z_{0})
$$
**Геометрический смысл аргумента производной:** $\arg f'(z_{0})$ - это угол, на который надо повернуть касательную к кривой $l$ в точке $z_{0}$ для того, чтобы получить направление касательной к кривой $L$ в точке $w_{0}$. Другими словами, $\arg f'(z_{0})$ - это угол между отображением и первоначальным направлениями касательных к кривых $l$ и $L$ в точках $z_{0}$ и $w_{0}$.

